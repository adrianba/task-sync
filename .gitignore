node_modules/
dist/
coverage/

# Local config & secrets — never commit
config.local.json
config.json
*.local.json
.env
.env.*

# Runtime data volumes (when run outside Docker)
.task-sync/
data/

# Editor / OS
.DS_Store
*.log
